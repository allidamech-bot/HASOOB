{
  "installCommand": "git clone https://github.com/flutter/flutter.git -b stable --depth 1 .vercel/flutter && ./.vercel/flutter/bin/flutter config --enable-web && ./.vercel/flutter/bin/flutter pub get",
  "buildCommand": "./.vercel/flutter/bin/flutter build web --release",
  "outputDirectory": "build/web",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}